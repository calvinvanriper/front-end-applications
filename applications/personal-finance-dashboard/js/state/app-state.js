export const appState = {
  // Stores the pending callback for confirmation modal actions.
  pendingConfirmationAction: null,

  // Stores the most recent successful conversion for add-to-watchlist flow.
  latestSuccessfulConversion: null,
};
